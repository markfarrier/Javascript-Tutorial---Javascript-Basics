// quotation marks
// "" or ''
// also ``

// can use / as an escape character
// can use single quotes inside double quotes
// can use double quotes inside single quotes
// likewise, you can use " and ' inside ` quotes

const name = "john's courses are the best";

const a = 'mark is "a person"';

const t = `here's a "test" 'scenario'`;

console.log(name);

console.log(a);

console.log(t);
